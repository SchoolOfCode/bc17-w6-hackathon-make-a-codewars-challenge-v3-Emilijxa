/* Create a function name 'sumResources' that takes an array of numbers (resource values) as an argument.
 
 * The function should return the sum of all the numbers in the array.
 
 * If the array is empty, function should return 0.
 
 * Your functions should handle arrays with negative numbers.
git
       Examples:
           - `sumResources([2, 4, 1, 3, 7])` should return 17 as the total sum of resources. 
           - `sumResources([-5, 8, 2, 6])` should return 11, accounting the negative number. */


export function sumResources(resources) {
    // Good luck!
}
