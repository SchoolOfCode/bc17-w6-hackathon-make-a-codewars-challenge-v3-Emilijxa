/* Hello Space Traveler! Create a function name 'sumResources' that takes an array of numbers 
   (resource values) as an argument.
 
 * The function should return the sum of all the numbers in the array.
 
 * If the array is empty, function should return 0.
 
 * Your functions should handle arrays with negative numbers.

       Examples:
           - `sumResources([2, 4, 1, 3, 7])` should return 17 as the total sum of resources. 
           - `sumResources([-5, 8, 2, 6])` should return 11, accounting the negative number. */


export function sumResources(resources) {
    // Good luck!
}










/* Well done if you reached this next challenge! Now you need to create a function named 
   'convertTemperature'.
   
 * The function should takes in a temperature in Celsius and return that temperature in Fahrenheit.

 * Hint: you might need to google to find out the conversion formula! 

       Examples:
           - `convertTemperature(0)` should return 32. 
           - `convertTemperature(60)` should return 140.
           - `convertTemperature(-50)` should return -58. */

export function convertTemperature(celsius) {
    // Good luck!
}
